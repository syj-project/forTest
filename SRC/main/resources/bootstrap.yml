spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/syj-project/forTest
          username: syj-project
          password: w15129106904
          default-label: master
          search-paths: src/main/resources

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS